import React from "react";

const ConfirmedBooking = (props) => {
    return (
        <div>
            Booking has been <strong>confirmed!</strong>
        </div>
    );
};

export default ConfirmedBooking;